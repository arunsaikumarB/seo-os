---
description: After frontend changes, sync apps/web to GitLab ba-frontend
alwaysApply: true
---

# GitLab frontend sync

When you change **frontend** code (`apps/web/**` or `packages/shared/**` used by the web app), after committing to GitHub `master` also sync to GitLab:

```powershell
powershell -File scripts/sync-frontend-to-gitlab.ps1 -Message "<same conventional message>"
```

- Remote: `http://gitlab.lstech-hq.lstechinc.com/websites/backlink-agent.git`
- Branch: `ba-frontend`
- Do **not** push API/backend to `ba-frontend` (that is `ba-backend`)
- Skip sync for docs-only / API-only / companion-only changes
