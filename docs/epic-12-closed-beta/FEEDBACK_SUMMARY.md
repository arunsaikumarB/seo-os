# Feedback Summary — framework

Feedback is stored in `beta_feedback` with:

- Types: bug · feature · general
- Severity · category · environment JSON · optional screenshot URL
- Status workflow: open → triaged → in_progress → resolved

Platform event: `beta_feedback_submitted`.

**Live cohort summary:** empty at ship time — populate from Beta Dashboard after invites.
